{
  "name": "mica-lightpanda-webdriver-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Thin W3C WebDriver-classic surface over Lightpanda's CDP endpoint, so mica's POST /session create flow works unchanged.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer-core": "^23.11.1"
  },
  "engines": {
    "node": ">=20"
  }
}
