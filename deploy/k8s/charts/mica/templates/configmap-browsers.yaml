apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mica.fullname" . }}-browsers
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mica.labels" . | nindent 4 }}
data:
  browsers.json: |
    {{- toJson .Values.browsersConfig | nindent 4 }}
